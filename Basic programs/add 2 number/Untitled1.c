#include <stdio.h>
 void main()
{
    int a,b,sum; /* variables declaration*/
    printf("\n Enter the values for a and b: \n");
    scanf("%d,%d", &a, &b);//seprated with comma
    sum=a+b;
    printf("\nThe sum if %d and %d is %d",a,b,sum); /*output statement*/
}
