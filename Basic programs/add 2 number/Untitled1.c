#include <stdio.h>
 void main()
{
    int a,b,sum;
    printf("\n Enter the values for a and b: ");
    scanf("%d%d", &a, &b);
    sum=a+b;
    printf("\nThe sum of %d and %d is %d",a,b,sum);
    getch();
}
